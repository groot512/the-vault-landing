# THE VAULT 작업순서 및 작업기록

## 1. 통합 작업 환경 원칙

GitHub 원격 저장소를 유일한 원본으로 사용한다. Mac과 Windows 사이에서 프로젝트 폴더 자체를 Google Drive, iCloud 또는 OneDrive로 동기화하지 않는다. 각 장비에서 저장소를 별도로 clone하고 Git으로만 변경사항을 교환한다.

### 권장 환경

- Mac 노트북: 디자인 확인, Safari 검증, 이미지 자산 정리
- Windows 집 PC: Chrome·Edge 검증, Windows 글꼴과 줄바꿈 확인
- 공통 편집기: VS Code 또는 UTF-8과 EditorConfig를 지원하는 편집기
- 배포 원본: GitHub 기본 브랜치 `main`

## 2. 최초 1회 설정

각 장비에서 다음 항목을 동일하게 설정한다.

1. Git과 편집기를 설치한다.
2. GitHub 계정 인증을 완료한다.
3. 저장소를 장비별 로컬 작업 폴더에 clone한다.
4. 저장소 안에서 `git config core.autocrlf false`를 설정한다.
5. `.gitattributes`와 `.editorconfig`가 적용되는지 확인한다.
6. 작업 폴더 이름은 영문 또는 한글 모두 가능하지만, 저장소 내부 배포 자산 경로는 영문 소문자로 유지한다.

Windows에서도 LF 줄바꿈을 사용한다. 한 장비에서만 파일명 대소문자를 변경하는 작업은 피하고, 필요한 경우 Git에서 명시적으로 이름을 변경한다.

## 3. 작업 시작 순서

1. 현재 작업 내용을 모두 저장한다.
2. `main`으로 이동한다.
3. `git status`로 미완료 변경이 없는지 확인한다.
4. `git pull --ff-only`로 원격 변경을 받는다.
5. 작업 단위별 브랜치를 만든다.
   - 예: `codex/hero-opening`
   - 예: `codex/brand-philosophy`
6. 로컬 정적 서버에서 작업한다.
7. 한 번에 하나의 섹션만 수정한다.

## 4. 작업 종료 순서

1. 변경 파일 목록을 확인한다.
2. 브라우저와 정적 검증을 완료한다.
3. 문서의 작업 기록을 갱신한다.
4. 필요한 파일만 stage한다.
5. 하나의 목적이 드러나는 커밋 메시지를 작성한다.
   - 예: `feat: implement cinematic hero entrance`
   - 예: `docs: define cross-device workflow`
6. 작업 브랜치를 GitHub에 push한다.
7. Pull Request에서 Mac과 Windows 확인 결과를 기록한다.
8. 승인 후 `main`에 병합한다.
9. 다른 장비에서는 다음 작업 전 `git pull --ff-only`를 실행한다.

## 5. 충돌 예방 규칙

- 두 장비에서 동일한 브랜치를 동시에 수정하지 않는다.
- 이미지 원본은 덮어쓰지 않고 새 파일명으로 추가한다.
- `index.html`, `styles.css`, `script.js`를 동시에 여러 브랜치에서 수정할 경우 먼저 작업 범위를 합의한다.
- 자동 포맷팅만 포함된 대규모 변경을 기능 변경과 같은 커밋에 넣지 않는다.
- `.DS_Store`, `Thumbs.db`, 편집기 설정과 임시 렌더 파일은 커밋하지 않는다.
- 원격에 push되지 않은 작업을 다른 장비에서 이어서 하지 않는다.

## 6. GitHub Pages 릴리스 순서

1. PR이 `main`에 병합되었는지 확인한다.
2. 정적 상대 경로와 파일명 대소문자를 확인한다.
3. GitHub Pages 배포 상태를 확인한다.
4. 배포 URL을 Mac Safari와 Windows Edge에서 각각 확인한다.
5. 문제가 있으면 `main`에서 직접 수정하지 않고 hotfix 브랜치를 만든다.

## 7. 작업 기록 형식

각 작업이 끝날 때 아래 형식으로 한 항목을 추가한다.

```text
날짜:
작업 장비:
브랜치:
작업 목적:
변경 파일:
검증 결과:
남은 이슈:
다음 작업:
```

## 8. 작업 기록

### 2026-08-14 — 저장소 기반 및 Hero 1차 구현

- 작업 장비: Mac
- 브랜치: `main` 초기 작업 상태
- 작업 목적: GitHub Pages용 정적 프로젝트 기반과 Cinematic Hero 구현
- 변경 파일:
  - `index.html`
  - `styles.css`
  - `script.js`
  - `assets/hero-security-room.jpg`
  - `.gitattributes`
  - `.editorconfig`
  - `.gitignore`
  - 프로젝트 계획 및 작업 기록 문서
- 구현 내용:
  - 100svh Hero
  - 4줄 비대칭 headline
  - 지정된 entrance timing
  - Lenis 초기화와 requestAnimationFrame 루프
  - Desktop 미세 패럴랙스
  - Mobile 전용 타이포그래피와 이미지 크롭
  - reduced motion 대응
- 남은 이슈:
  - 독립된 벡터 로고 필요
  - 실제 기기별 시각 검토 필요
  - GitHub 원격 저장소와 Pages 설정 필요
- 다음 작업: Hero 시각 검토 및 승인 후 Brand Philosophy 설계 착수

### 2026-08-14 — Brand Philosophy 구현

- 작업 장비: Mac
- 브랜치: `main` 초기 작업 상태
- 작업 목적: Hero를 유지하면서 Brand Philosophy 단일 섹션 추가
- 변경 파일:
  - `index.html`
  - `styles.css`
  - `script.js`
  - `assets/philosophy-office.mp4`
  - 프로젝트 계획 및 작업 기록 문서
- 구현 내용:
  - Desktop 2-column editorial layout
  - 우측 oversized typography sticky 처리
  - `OUR / PHILO / SOPHY` 글자별 미세 scatter-to-align
  - 좌측 영상의 세로 패럴랙스와 가시성 기반 재생 제어
  - Mobile sticky·scatter·패럴랙스 제거
  - reduced motion에서 영상과 동작 정지
- 영상 출처: Tima Miroshnichenko / Pexels
- 남은 이슈: 실제 브랜드 영상 확보 시 임시 영상을 교체
- 다음 작업: 나머지 브랜드 스토리 섹션 구현

### 2026-08-14 — 원페이지 전체 섹션 구현 및 1차 검증

- 작업 장비: Mac
- 브랜치: `main` 초기 작업 상태
- 작업 목적: Vision부터 CTA / Footer까지 남은 섹션을 완성하고 전체 반응형 동작 검증
- 변경 파일:
  - `index.html`
  - `styles.css`
  - `script.js`
  - `assets/application-floor-plan.jpg`
  - `assets/application-secure-pod.jpg`
  - `assets/application-vault-room.jpg`
  - `assets/application-business-card.jpg`
  - 프로젝트 계획 및 작업 기록 문서
- 구현 내용:
  - Vision의 대형 타이포그래피와 스크롤 축 전환
  - THE VAULT ROOM, TESSERA, DIGITAL VAULT 3단계 Approach
  - Brand Story의 sticky 선언문과 4개 챕터
  - Visual Identity의 워드마크, 원리, 팔레트, 명함 적용
  - Brand Applications의 무한 마키와 가로 이미지 내러티브
  - CTA / Footer와 섹션 내비게이션
  - Pretendard Variable 동적 서브셋 적용
  - Desktop과 Mobile을 분리한 레이아웃 및 모션 규칙
  - reduced motion, no-JavaScript, Lenis CDN 실패 시 fallback
- 검증 결과:
  - HTML 섹션 8개와 로컬 자산 경로 확인
  - JavaScript 구문 및 Git diff whitespace 검사 통과
  - 1440 × 900 데스크톱 주요 스크롤 장면과 sticky 전환 확인
  - 320, 390, 768, 1440, 1920px 경계 폭 확인
  - Mobile sticky 해제, Approach 3개 챕터 노출, 수평 overflow 차단 확인
  - 로컬 HTML과 모든 자산의 HTTP 200 응답 확인
  - 브라우저 콘솔 경고 및 오류 없음
- 남은 이슈:
  - 독립된 벡터 로고와 최종 브랜드 영상 교체
  - Windows Edge와 macOS Safari 실기기 교차 확인
  - GitHub 원격 저장소 연결 및 Pages 설정
- 다음 작업: 원격 저장소 생성 후 작업 브랜치 커밋, PR, GitHub Pages 배포

### 2026-08-14 — Hero 스크롤 내러티브 복원

- 작업 장비: Mac
- 브랜치: `main` 초기 작업 상태
- 작업 목적: 시간 기반으로 고정돼 있던 Hero headline을 원래 의도한 스크롤 기반 opening sequence로 복원
- 변경 파일:
  - `index.html`
  - `styles.css`
  - `script.js`
  - 프로젝트 계획 및 작업 기록 문서
- 구현 내용:
  - Desktop `210svh`, Mobile `165svh` 스크롤 runway
  - `100svh` sticky stage
  - 배경 focus, logo, eyebrow는 기존 로드 진입 타이밍 유지
  - headline 4줄을 Hero 진행률 `0 → 1`에 순차 매핑
  - headline 완성 이후 statement와 subtle shine 노출
  - 스크롤 방향을 되돌렸을 때 애니메이션도 역방향으로 복귀
  - scroll cue의 진입, 퇴장, 상단 복귀 상태 처리
  - reduced motion과 no-JavaScript 환경에서는 runway를 제거하고 모든 정보 즉시 노출
- 검증 결과:
  - Desktop 초기·중간·완성·다음 섹션 전환 상태 확인
  - Mobile 초기·완성·역방향 복귀 상태 확인
  - sticky stage, headline별 opacity, 수평 overflow 수치 확인
  - 브라우저 콘솔 경고 및 오류 없음
- 남은 이슈: 실제 사용자의 스크롤 감각에 따라 runway 길이와 line reveal 간격을 미세 조정 가능
- 다음 작업: 사용자 시각 확인 후 Git 작업 또는 배포 진행

### 2026-08-15 — 제품 페이지 및 VAULT TRUST LAB MVP

- 작업 장비: Mac
- 브랜치: `main` 로컬 작업 상태
- 작업 목적: THE VAULT 랜딩에서 TESSERA와 DIGITAL VAULT 제품 페이지로 이어지는 브랜드·제품 구조를 구현하고, 투자 미팅용 기능성 암호화 MVP 구축
- 변경 파일:
  - `index.html`
  - `styles.css`
  - `products.css`
  - `products.js`
  - `tessera/index.html`
  - `digital-vault/index.html`
  - `app/index.html`
  - `app/app.css`
  - `app/app.js`
  - `app/README.md`
  - `TESSERA-DIGITAL-VAULT-MVP-실행계획.MD`
  - 작업 기록 문서
- 구현 내용:
  - 기존 Approach 섹션에서 두 제품 페이지로 이동하는 CTA
  - `by THE VAULT` 공통 브랜드 체계
  - TESSERA와 DIGITAL VAULT 독립 제품 페이지
  - Web Crypto AES-GCM 실제 브라우저 암호화 Proof
  - Desktop sticky 3단계 Trust Kernel 스토리
  - Mobile 전용 정적 스토리와 타이포그래피 배치
  - 네트워크 요청 없는 VAULT TRUST LAB
  - 메시지 암호화·복호화 round-trip
  - 실제 로컬 파일 암호화·복호화와 파일 키 폐기
  - 콘텐츠를 포함하지 않는 감사 이벤트
  - Prototype 페이지의 `noindex, nofollow`와 개발 경계 고지
- 검증 결과:
  - JavaScript 구문 검사와 Git whitespace 검사 통과
  - `/`, `/tessera/`, `/digital-vault/`, `/app/` 및 정적 자산 HTTP 200
  - 1440 × 900 Desktop 제품 Hero, Proof, sticky chapter 확인
  - 390 × 844 Mobile 레이아웃과 수평 overflow 수정·확인
  - 메시지 암호화 상태 `Verified` 및 입력 초기화 확인
  - 18.4KB Markdown 파일 암호화와 암호문 표시 확인
  - 파일 키 폐기 후 복호화 버튼 비활성화 확인
  - 감사 로그에 테스트 메시지 평문과 파일명이 포함되지 않음을 확인
  - 전체 페이지 브라우저 콘솔 경고 및 오류 없음
- 현재 경계:
  - 로컬 브라우저 메모리 기반 기술 시연이며 서버, 계정, 영구 저장 없음
  - 상용 E2EE, 보안 인증 또는 규제 준수 제품이 아님
- 다음 작업:
  - Matrix 메시징 및 Supabase 조직·스토리지 기술 스파이크
  - 공식 도메인과 Prototype 공개 범위 결정
  - 디자인 파트너 인터뷰 및 Private Pilot 요구사항 수집

### 2026-08-15 — 브랜드 페이지 글로벌 내비게이션

- 작업 장비: Mac
- 브랜치: `main`
- 작업 목적: 모회사 브랜드 페이지와 제품 페이지 사이의 정보 구조를 상단 내비게이션으로 연결
- 변경 파일:
  - `index.html`
  - `styles.css`
  - `script.js`
- 구현 내용:
  - Philosophy, Vision, Approach 내부 앵커
  - TESSERA, DIGITAL VAULT, VAULT TRUST LAB 제품·프로토타입 링크
  - 네 페이지에서 동일하게 작동하는 고정형 charcoal 글로벌 내비게이션
  - Desktop 서비스 hover·keyboard focus 세부 메뉴
  - TESSERA와 DIGITAL VAULT의 Overview, Proof, Architecture, Status 바로가기
  - Trust Lab의 Tessera Proof, Vault Proof, Audit 바로가기와 hash 기반 화면 전환
  - 현재 제품을 gold marker로 표시하는 위치 정보
  - Hero와 겹치지 않는 상단 배치
  - 기존 Hero 내부 워드마크 중복 제거 및 스크롤 시퀀스 유지
  - Mobile 전용 전체 화면 메뉴와 서비스별 accordion
  - ESC·링크 선택·화면 전환 시 메뉴 닫힘 처리
  - 고정 내비게이션 높이를 반영한 앵커 오프셋과 reduced-motion 대응
- 검증 결과:
  - Desktop 1440 × 900에서 브랜드 페이지와 TESSERA 페이지 메뉴 폭·현재 위치 확인
  - Mobile 390 × 844에서 전체 메뉴와 accordion 열림·닫힘, 수평 overflow 0 확인
  - Trust Lab `#signal`, `#archive`, `#audit` 직접 링크 화면 전환 확인
  - Hero 초기 opacity 0 → 스크롤 후 4줄 opacity 1, sticky stage 유지 확인

### 2026-08-25 — 관리자 VAULT 계정 발급 함수와 트러스트 랩 UI

- 작업 장비: Mac
- 브랜치: `codex/supabase-connect`
- 작업 목적: 실명·이메일·휴대폰 번호를 수집하지 않는 초대 전용 계정 발급 흐름을 서버 권한 검증과 관리자 화면으로 연결
- 변경 파일:
  - `app/index.html`
  - `app/app.css`
  - `app/app.js`
  - `app/identity.js`
  - `app/README.md`
  - `supabase/functions/issue-vault-account/index.ts` — 공개 저장소 제외 서버 원본
  - `supabase/README.md` — 공개 저장소 제외 내부 기록
  - 작업 기록 문서
- 구현 내용:
  - Supabase `issue-vault-account` Edge Function 생성 및 배포
  - 로그인 사용자 JWT, TOTP AAL2, 요청 조직의 `admin` 멤버십을 서버에서 순서대로 검증
  - 무작위 VAULT ID와 20자 임시 비밀번호 생성
  - Auth 사용자 생성 후 조직 `member` 역할 연결
  - 멤버십 생성 실패 시 Auth 사용자 자동 삭제
  - 비밀번호 원문을 별도 테이블이나 로그에 저장하지 않고 발급 응답에서 한 번만 전달
  - 트러스트 랩에 관리자에게만 노출되는 `계정 발급` 메뉴와 Access Office 화면 추가
  - 오프라인 전달 확인 체크, 발급 결과 1회 표시, 개별 복사, 화면 폐기 기능
  - 계정 발급 이벤트는 자격증명 없이 로컬 감사 기록에 유형만 추가
  - 관리자 화면 이탈 시 표시 중인 임시 자격증명을 메모리와 DOM에서 제거
- 검증 결과:
  - 최초 배포가 화면상 성공했지만 실행 시 `BOOT_ERROR`가 발생하는 것을 실제 호출로 발견
  - Supabase 편집기 런타임 형식과 중복으로 남아 있던 템플릿 코드를 제거하고 단일 147줄 함수로 재배포
  - CORS preflight `OPTIONS` 요청이 HTTP 204로 정상 응답
  - 인증 헤더 없는 `POST` 요청이 HTTP 401로 차단
  - `app.js`, `identity.js`, `i18n.js` JavaScript 구문 검사 통과
  - Git whitespace 검사 통과
  - 로컬 HTTP 로그인 화면에서 비관리자·미인증 상태에 관리자 메뉴와 화면이 노출되지 않음을 확인
- 아직 실행하지 않은 검증:
  - 유효한 관리자 세션에서의 실제 계정 1건 발급은 Supabase Auth 사용자와 멤버십을 생성하므로 수행하지 않음
  - 배포된 GitHub Pages의 관리자 화면 데스크톱·모바일 실사용 검증
- 후속 수정:
  - `file://`로 실행한 로컬 앱의 브라우저 요청 Origin이 `null`로 전달되어 `Origin not allowed`가 발생함
  - 서버의 AAL2·조직 관리자 검증은 유지하면서 로컬 파일 미리보기의 `null` Origin을 허용 목록에 추가
- 사용자 확인 결과:
  - Supabase Authentication 사용자 생성 확인
  - `profiles`의 발급 VAULT ID와 Auth 사용자 ID 연결 확인
  - `memberships`의 동일 사용자 ID와 `member` 역할 확인
  - `organization_id`가 관리자 조직과 연결된 것을 확인
  - 관리자 계정 발급 → Auth 사용자 → 프로필 → 조직 멤버십 생성 흐름 검증 완료
  - 발급된 임시 VAULT ID와 비밀번호로 신규 사용자 최초 로그인 완료
  - 신규 비밀번호 변경, TOTP 인증 앱 등록, 복구키 발급·확인 완료
  - 신규 사용자가 `MEMBER / SUPABASE` 권한으로 작업공간에 진입하는 Golden Path 완료
  - 신규 사용자에게 관리자 전용 계정 발급 기능이 노출되지 않는 권한 분리 확인
- 다음 작업:
  - 관리자 로그인 후 구성원 접근 정지·복구 Golden Path 검증
  - 계정 발급·권한 변경·폐기 감사 이벤트를 서버 테이블로 확장

### 2026-08-25 — 구성원 접근 생명주기와 공개 페이지 배포

- 작업 장비: Mac
- 브랜치: `codex/supabase-connect`
- 작업 목적: 발급된 계정을 삭제하지 않고 조직 관리자가 접근을 안전하게 정지·복구할 수 있는 다음 단계 구현
- 변경 파일:
  - `app/index.html`
  - `app/app.css`
  - `app/app.js`
  - `app/identity.js`
  - `app/README.md`
  - `supabase/functions/manage-vault-members/index.ts` — 공개 저장소 제외 서버 원본
  - `supabase/config.toml` — 공개 저장소 제외 내부 설정
  - `supabase/README.md` — 공개 저장소 제외 내부 기록
  - 작업 기록 문서
- 구현 내용:
  - Supabase `manage-vault-members` Edge Function 생성 및 배포
  - 호출자의 JWT, TOTP AAL2, 요청 조직의 `admin` 멤버십을 서버에서 검증
  - 관리자 화면에 VAULT ID, 역할, 활성 상태, 가입일, 최근 로그인일만 표시
  - 일반 `member` 계정의 로그인 정지와 복구 제공
  - 계정 삭제 대신 장기 ban을 사용해 복구 가능성 유지
  - 관리자 자신의 계정과 다른 관리자 계정은 변경할 수 없도록 서버에서 차단
  - 정지·복구 시 Auth `app_metadata`에 접근 상태와 변경 시각·변경 관리자 ID 기록
  - 앱을 다시 열 때 서버 사용자 상태를 재검증하고 정지 계정의 기존 로컬 세션을 종료
  - 정지·복구 이벤트는 대상 자격증명이나 콘텐츠 없이 로컬 감사 기록에 유형만 추가
- 검증 결과:
  - Edge Function CORS preflight `OPTIONS` 요청 HTTP 204
  - 인증 헤더 없는 `POST` 요청 HTTP 401 차단
  - `app.js`, `identity.js` JavaScript 구문 검사 통과
  - Git whitespace 검사 통과
  - 로컬 HTTP에서 HTML, CSS, `app.js`, `identity.js` HTTP 200 확인
  - 로컬 로그인 화면 브라우저 콘솔 오류 없음
  - GitHub Pages 배포 실행 `32751034402` build·deploy 성공
  - 공개 `/app/`에서 캐시 버전 `260825-2`, 구성원 목록 마크업, 접근 관리 호출 코드 제공 확인
- 아직 실행하지 않은 검증:
  - 실제 관리자 세션의 구성원 목록 조회
  - 테스트 멤버 1건의 접근 정지 → 로그인 차단 → 접근 복구 → 재로그인 Golden Path
  - 이 검증은 실제 사용자 인증 상태를 변경하므로 관리자 화면에서 대상을 확인한 뒤 수행

### 2026-08-28 — NCP 단일 서버 전환 사전 점검

- 작업 장비: Mac
- 브랜치: `codex/supabase-connect`
- 작업 목적: 외부 백엔드 의존성을 제거하고 THE VAULT 웹·인증·데이터베이스·파일 저장소를 하나의 NCP 서버에서 운영할 수 있는지 검증
- NCP 서버:
  - 공인 IP: 공개 저장소 문서에는 기록하지 않고 NCP 콘솔과 로컬 SSH 설정에서 관리
  - 운영체제: Ubuntu 24.04.1 LTS
  - CPU: 1 vCPU
  - 메모리: 961 MiB
  - 스왑: 없음
  - 루트 디스크: 10 GB 중 약 5.2 GB 사용 가능
  - 초기 열린 주요 포트: SSH 22, rpcbind 111
- 접속 구성:
  - NCP가 발급한 관리자 이름과 초기 비밀번호로 `root` 원격 접속 확인
  - 로컬 PEM의 공개키를 `/root/.ssh/authorized_keys`에 등록
  - PEM 지문: 로컬에서 별도 확인하며 공개 저장소 문서에는 기록하지 않음
  - PEM 기반 비대화식 SSH 접속 검증 완료
- 현재 애플리케이션 백엔드 의존성 확인:
  - Supabase Auth VAULT ID 로그인
  - 비밀번호 변경과 TOTP MFA/AAL2
  - `profiles`, `organizations`, `memberships`, `devices`, `recovery_credentials`
  - Postgres RLS와 복구키 검증 RPC
  - 계정 발급·구성원 접근 관리 Edge Functions
  - 브라우저 기기 키와 콘텐츠 암호화는 클라이언트에 유지
- 판단:
  - 현재 1 vCPU / 1 GB / 10 GB 서버는 Supabase 전체 Docker 스택을 안정적으로 자체 호스팅하기에 부족함
  - 무리한 설치 전 서버 증설형과 경량 맞춤 백엔드형을 비교해 운영 경로를 확정해야 함
  - 기존 관리형 Supabase는 전환 검증이 끝날 때까지 롤백 원본으로 유지
- 다음 결정:
  - 권장안: 최소 2 vCPU / 4 GB RAM / 40~50 GB 디스크로 증설 후 자체 호스팅 Supabase와 리버스 프록시 구성
  - 대안: 현 서버에 경량 API·데이터베이스·로컬 암호문 저장소를 새로 구현하되, 기존 Supabase 전용 인증·RLS·함수 코드를 별도 이식

### 2026-08-30 — NCP 프론트엔드·Supabase 백엔드 분리 배포

- 작업 장비: Mac
- 브랜치: `codex/supabase-connect`
- 확정 구조:
  - NCP: 브랜드 랜딩페이지, 제품 페이지, VAULT TRUST LAB 정적 프론트엔드
  - Supabase: Auth, TOTP/AAL2, Postgres/RLS, 관리자 Edge Functions
  - GitHub: 코드와 배포 설정의 단일 원본
  - 도메인·HTTPS 전까지 GitHub Pages를 운영본으로 유지하고 NCP는 스테이징으로 사용
- 추가 파일:
  - `deploy/ncp/publish.sh`
  - `deploy/ncp/nginx.conf`
  - `deploy/ncp/README.md`
  - `SUPABASE-NCP-초보자-학습가이드.MD`
- 배포 설계:
  - HTML, CSS, JavaScript, 공개 이미지·영상과 제품 디렉터리만 릴리스에 포함
  - Markdown 작업문서, Git 정보, PEM·환경 파일은 웹 루트에서 제외
  - UTC 시각별 새 릴리스를 만든 뒤 `current` 심볼릭 링크를 교체
  - Nginx에 CSP, frame 차단, MIME sniffing 차단, referrer·권한 정책 적용
  - CSP의 네트워크 연결은 현재 Supabase HTTPS·WSS 프로젝트로 제한
- NCP 작업:
  - Ubuntu 저장소 패키지 목록 갱신
  - Nginx 1.24.0 설치 및 부팅 자동 시작
  - IPv6 미지원 서버 특성에 맞춰 IPv4 80 포트만 사용
  - `/var/www/the-vault/releases/{UTC 시각}`에 첫 정적 릴리스 배치
- 검증 결과:
  - 로컬 배포 스크립트 문법과 필수 파일·문서 제외 검사 통과
  - NCP `nginx -t` 통과 및 서비스 `active`
  - 서버 내부 `/`, `/tessera/`, `/digital-vault/`, `/app/`, Hero 이미지 HTTP 200
  - 보안 응답 헤더와 CSP 제공 확인
  - Nginx 80과 SSH 22 리슨 확인
  - 디스크 사용량 49%, 약 4.8GB 여유
- 외부 공개 검증:
  - ACG 규칙에 80 대신 88이 입력되고 최종 적용이 누락된 것을 화면으로 확인
  - TCP `0.0.0.0/0`의 80번 규칙으로 수정·적용 후 외부 접속 성공
  - 외부 `/`, `/tessera/`, `/digital-vault/`, `/app/`, Hero 이미지 HTTP 200
  - 실제 1280 × 720 브라우저에서 랜딩과 Brand Philosophy 렌더링 및 수평 overflow 0 확인
  - 브라우저 콘솔 오류·경고 없음
  - `http://IP`는 `isSecureContext=false`, Web Crypto 비활성 상태임을 확인
- 현재 경계:
  - IP 기반 HTTP는 랜딩·제품 페이지 스테이징 확인에만 사용
  - Trust Lab 로그인·암호화 검증은 HTTPS 적용 전 실행하지 않음
  - 서버에 설치 가능한 업데이트 343건이 남아 있어 운영 전 보안 업데이트 필요
- 다음 작업:
  - 공식 도메인 A 레코드 연결과 HTTPS 인증서 적용
  - Supabase Edge Function 허용 Origin에 공식 도메인 추가
  - HTTPS 환경에서 로그인·TOTP·계정 발급·구성원 관리 재검증

### 2026-08-31 — NCP ACG 재확인 및 운영체제 보안 업데이트

- 작업 장비: Mac
- 브랜치: `codex/supabase-connect`
- ACG 확인:
  - 사용자가 잘못 입력된 88번 포트를 80번으로 수정하고 저장
  - 외부 네트워크에서 `/`와 `/app/` HTTP 200 재확인
- SSH 안전 확인:
  - 서버의 RSA·ED25519 호스트 키 지문을 Mac의 기존 `known_hosts`와 대조
  - 두 지문이 일치한 뒤 PEM 키 기반 접속 재개
- 보안 업데이트:
  - Ubuntu 패키지 261개 업그레이드
  - 이전 업데이트가 `/etc/issue` 설정 질문에서 중단되어 다수 패키지가 미설정 상태였음
  - NCP 기존 로그인 안내 설정을 보존하는 `force-confold` 정책으로 `dpkg` 상태 복구
  - OpenSSH, OpenSSL, Nginx, CA 인증서, libc, AppArmor 등 주요 패키지 갱신
- 업데이트 후 검증:
  - `dpkg --audit` 미설정 패키지 없음
  - SSH와 Nginx 모두 `active`
  - `nginx -t` 통과
  - 서버 내부 `/` HTTP 200
  - 외부 `/`, `/app/` HTTP 200
  - 루트 디스크 61% 사용, 약 3.7GB 여유
  - 메모리 약 567MiB 사용 가능, 스왑 없음
- 남은 항목:
  - libc6·AppArmor 새 버전 반영을 위해 서버 재부팅 완료
  - 재부팅 후 커널 `6.8.0-124-generic`, 재부팅 추가 요구 없음
  - SSH·Nginx 자동 복구와 `/`, `/tessera/`, `/digital-vault/`, `/app/` 외부 HTTP 200 재검증
  - 재부팅 후 실패한 systemd 서비스 없음
  - 자동 보류된 `fwupd`, `linux-generic`, `linux-headers-generic`, `linux-image-generic`, `sosreport` 5개는 Ubuntu 의존성·단계 배포 정책에 따라 별도 검토
  - `full-upgrade` 모의 계산 결과 기존 패키지 제거는 없지만 커널 메타 패키지 3개는 계속 보류되므로 강제 적용하지 않음
  - 오래된 커널 자동 삭제는 복구 가능성을 위해 아직 실행하지 않음

### 2026-08-31 — GitHub → NCP 자동 배포 구성

- 선택한 방식: NCP가 GitHub `main`을 1분마다 확인하는 pull 방식
- 선택 이유:
  - GitHub Secrets에 NCP SSH 개인키를 저장하지 않음
  - GitHub Actions 접속을 위해 SSH 22번을 전 세계에 개방할 필요가 없음
  - 작은 NCP 서버에서도 별도 runner 없이 가볍게 동작
- 추가 구성:
  - `deploy/ncp/auto-deploy.sh`: 새 커밋 확인, 안전 검사, fast-forward, 릴리스 발행
  - `deploy/ncp/the-vault-deploy.service`: 저권한 자동 배포 작업
  - `deploy/ncp/the-vault-deploy.timer`: 1분 주기 실행
- 안전 경계:
  - `main` 브랜치가 아니거나 로컬 변경이 있으면 배포 중단
  - Git 이력이 fast-forward가 아니면 배포 중단
  - 로그인 셸이 없는 `vault-deploy` 사용자로 실행
  - 서버 전체가 아닌 저장소·웹 릴리스·배포 상태 경로만 쓰기 허용
- 서버 적용:
  - `vault-deploy` 시스템 사용자와 전용 상태 디렉터리 생성
  - 자동 배포 스크립트는 root 소유로 고정하고 배포 작업만 `vault-deploy` 권한으로 실행
  - Git 저장소와 정적 릴리스 경로를 배포 사용자 소유로 전환
  - systemd unit 검증 후 타이머 부팅 자동 시작 활성화
- 1차 검증:
  - 서비스 즉시 실행으로 서버 저장소를 GitHub 커밋 `f07c054`까지 fast-forward
  - 새 정적 릴리스 생성 및 마지막 배포 커밋 상태 파일 기록 확인
  - 서비스 정상 종료와 다음 1분 타이머 예약 확인
- 최종 검증:
  - 작업 기록 커밋 `690af93`을 GitHub에 push
  - 별도 SSH 배포 명령 없이 약 1분 뒤 NCP가 새 커밋을 자동 감지
  - 서버 저장소 HEAD와 `/var/lib/vault-deploy/deployed-commit`이 모두 `690af93`으로 일치
  - 새 릴리스 생성과 서비스 정상 종료 로그 확인

### 2026-08-31 — 테세라 1:1 암호화 메시지 MVP

- 작업 장비: Mac
- 브랜치: `codex/supabase-connect`
- 작업 목적: 로컬 암호화 시연을 같은 조직의 두 사용자 기기 사이에서 실제로 주고받는 메시지 MVP로 확장
- 클라이언트 구현:
  - 조직 구성원의 활성 기기와 공개키 조회
  - 대화 상대 기기 선택과 기기 지문 표시
  - 두 ECDH P-256 기기 키로 공유 비밀 생성
  - 조직·기기 쌍을 컨텍스트로 HKDF-SHA256 콘텐츠 키 파생
  - 메시지마다 무작위 96비트 IV와 AES-256-GCM 암호화
  - Supabase에는 송수신 식별자, 기기 식별자, 알고리즘, IV, 암호문만 전송
  - 최근 100개 암호문 조회, 브라우저 복호화, Realtime INSERT 수신
  - 선택 기기 변경·연락처 새로고침·오류·빈 대화 상태 UI
  - 8,000바이트 평문 상한과 모바일 단일 열 레이아웃
- Supabase 구현:
  - `tessera_messages` 테이블 생성
  - 익명 역할 권한 제거, 로그인 사용자도 TOTP AAL2·조직 멤버십·대화 당사자 조건을 모두 만족해야 조회 가능
  - 송신자 본인, 양측 조직 멤버십, 양측 활성 기기 소유관계를 검증하는 INSERT RLS
  - 다른 조직 구성원의 활성 기기 공개키만 반환하는 `list_tessera_contacts` RPC
  - `supabase_realtime` publication 등록
  - 서버 마이그레이션 원본은 공개 저장소 제외 정책에 따라 로컬 `supabase/`에 보관
- 검증 결과:
  - Supabase SQL 실행 성공
  - DB 확인 쿼리에서 메시지 테이블·연락처 RPC 존재, RLS 정책 2개, Realtime 활성화 확인
  - publishable key만 사용한 익명 REST 조회가 HTTP 401로 차단됨을 확인
  - 서로 다른 두 P-256 키쌍에서 양방향 ECDH/HKDF 결과가 일치하고 AES-GCM 복호화 성공
  - 로컬 프리뷰에서 메시지 암호화·복호화 왕복, Boundary Inspector 암호문 표시, 콘솔 오류 없음
  - 390px 모바일에서 연락처 UI 단일 열 전환 및 수평 overflow 없음
  - JavaScript 구문 검사와 Git whitespace 검사 통과
- 현재 보안 경계:
  - 메시지 평문과 기기 개인 키는 브라우저 밖으로 전송하지 않음
  - 현재는 선택한 두 기기의 1:1 메시지만 지원
  - Double Ratchet·순방향 비밀성·다중 기기 동기화·키 교체 전파는 미구현
  - 상용 보안 제품이 아니라 투자·사용성 검증을 위한 기술 MVP
- 다음 검증:
  - 배포본에서 관리자·멤버 두 계정으로 기기 선택 → 전송 → Realtime 수신 → 재접속 후 복호화 Golden Path
  - 다른 조직·비당사자·AAL1 세션 접근 차단 테스트 자동화
- 배포 결과:
  - 기능 커밋 `dd433b4`를 GitHub `main`에 push
  - GitHub Pages 실행 `33353238638` build·deploy 성공
  - NCP pull 자동 배포가 같은 커밋을 감지해 새 릴리스 생성
  - NCP 배포 상태 커밋 `dd433b4`, 타이머 `active`, 서버 내부 `/app/` HTTP 200 확인
  - HTTPS GitHub Pages `/app/`에서 새 로그인 화면과 Supabase 연결, 브라우저 콘솔 오류 없음 확인
  - 브라우저에 로그인 세션과 자격증명을 남기지 않아 두 계정 실메시지 Golden Path는 사용자 로그인 후 진행 예정

### 2026-08-31 — 테세라 메신저 UX·조직 디렉터리 개편

- 작업 장비: Mac
- 브랜치: `codex/supabase-connect`
- 사용자 관찰:
  - 기존 Project Orbit·Boundary Inspector 중심 화면은 기술 시연에는 적합하지만 일반 메신저로 이해하기 어려웠음
  - 같은 Chrome 프로필의 두 탭에서 관리자·게스트로 각각 로그인하면 Supabase 인증 세션이 공유되어 한쪽 계정이 다른 쪽을 덮어쓸 수 있었음
  - 이 세션 충돌이 겉으로는 메시지 전송 실패처럼 보일 수 있어 별도 브라우저 또는 Chrome 프로필 테스트가 필요함
- UI 개편:
  - 왼쪽 조직 연락처 목록, 중앙 1:1 대화, 메시지 작성기의 익숙한 메신저 구조로 재배치
  - 본인 별명과 변경 불가 `@VAULT-ID`를 함께 표시
  - VAULT ID·별명 검색, 연락처 새로고침, 기기 미등록 상태 추가
  - 암호문·키 경계 등 기술 정보는 `보안 상세` 패널로 이동
  - 모바일은 축소형 3열이 아니라 연락처 목록 → 대화방 드릴다운 흐름으로 별도 구성
- 세션 안전장치:
  - 메시지 조회·전송·별명 변경 전에 Supabase 서버의 현재 사용자를 재확인
  - 다른 탭에서 계정이 바뀌면 `SESSION CONFLICT` 상태로 전환하고 전송·Realtime 구독을 중단
  - 사용자에게 다른 브라우저 또는 별도 Chrome 프로필 사용 안내
- Supabase 변경:
  - `profiles.nickname` 추가, 2~24자·제어문자 금지 제약 적용
  - AAL2 본인만 호출 가능한 `set_tessera_nickname(text)` 함수 추가
  - 같은 조직의 구성원을 VAULT ID·별명·활성 기기 정보와 함께 반환하는 `list_tessera_directory(uuid)` 함수 추가
  - 기존 `list_tessera_contacts`와 메시지 데이터는 삭제하지 않고 보존
  - 새 함수 실행 권한은 `authenticated` 역할로 제한
- 검증:
  - Supabase SQL 실행 성공, 콘솔 오류 없음
  - 읽기 전용 확인 쿼리에서 `nickname` 열·별명 함수·디렉터리 함수가 모두 `true`
  - 로컬 프리뷰에서 별명·사람 검색·연락처·대화·보안 상세 렌더링 확인
  - 로컬 메시지 암호화·복호화 왕복 및 경계 검사기 암호문 표시 확인
  - 390 × 844 모바일에서 목록/대화 전환과 수평 overflow 없음 확인
- Golden Path 진단·완료:
  - 첫 전송에서 Supabase `42501` 확인: `tessera_sender_insert` 정책이 상대방 활성 기기를 직접 조회하면서 `devices_select_own`에 의해 상대 기기 행을 볼 수 없었음
  - 멤버십·조직·양쪽 활성 기기는 모두 정상임을 읽기 전용 SQL로 확인
  - 다른 사용자의 기기 행을 클라이언트에 공개하지 않고 서버 내부에서만 경로를 확인하는 `is_valid_tessera_route(...)` SECURITY DEFINER 함수 추가
  - INSERT 정책을 AAL2 확인 + 서버 경로 검증 함수 조합으로 교체
  - 정책 함수 존재와 INSERT 정책 연결이 모두 `true`임을 확인
  - 게스트 → 관리자 `테세라 Golden Path 테스트` 전송 및 관리자 화면 실시간 수신 확인
  - 관리자 → 게스트 `관리자 응답 테스트` 전송 및 게스트 화면 Realtime 수신·복호화 확인
  - 새로고침 후 양방향 메시지를 서버 암호문에서 다시 불러와 브라우저에서 복호화하는 재접속 경로 확인
  - DB 최신 행에서 발신·수신 경로, 알고리즘, IV 길이, 암호문 길이만 확인되고 평문은 조회되지 않음
  - 남은 검증은 다른 조직·비당사자·AAL1·폐기 기기 차단 자동화
- 배포 결과:
  - 기능 커밋 `9cbb61f`를 GitHub `main`에 push
  - GitHub Pages 실행 `33358994588` build·deploy 성공
  - GitHub Pages와 NCP `/app/`에서 새 사람 검색 UI와 `260831-2` 자산 제공 확인
  - NCP 자동 배포 상태 커밋 `9cbb61f`, 타이머 `active`, 서버 내부 `/app/` HTTP 200 확인
  - HTTPS 배포본 브라우저 콘솔 오류·경고 없음
  - 전송 실패 원인을 모바일 작성창에서도 볼 수 있도록 오류 상태와 Supabase 오류 코드를 표시하는 커밋 `c1b7874` 배포

### 2026-09-01 — 테세라 보안 회귀 테스트 자동화

- 작업 목적:
  - 정상적인 메시지 전송만 허용되고 잘못된 접근은 계속 차단되는지 한 번의 실행으로 반복 검증
  - 향후 RLS 정책이나 메시지 기능을 수정해도 기존 보안 경계가 무너지지 않았는지 조기에 발견
- 테스트 방식:
  - 공개 저장소에서 제외한 `supabase/tests/tessera_security_regression.sql`에 서버 측 회귀 테스트 작성
  - 실제 조직의 admin·member와 활성 기기를 자동으로 찾아 테스트 조건 구성
  - `authenticated` 역할과 JWT 인증 수준을 바꾸어 실제 RLS 정책을 통과하는 경로로 검증
  - 전체 테스트를 하나의 데이터베이스 트랜잭션에서 실행하고 마지막에 `ROLLBACK`하여 시험 데이터와 상태 변경을 모두 취소
- 자동 검증 항목:
  - 같은 조직의 대화 당사자와 활성 기기, TOTP AAL2 세션은 메시지 저장 허용
  - 비밀번호 인증만 마친 AAL1 세션은 메시지 저장 차단
  - 다른 조직을 지정한 메시지는 저장 차단
  - 대화 비당사자는 시험 메시지를 조회할 수 없음
  - 수신 기기를 일시적으로 `revoked` 상태로 바꾼 경우 메시지 저장 차단
- 실행 결과:
  - 다섯 가지 허용·차단 조건 모두 통과
  - 최종 결과 `PASS: transaction rolled back; no test rows or device changes persisted` 확인
  - 별도 읽기 전용 확인에서 남은 시험 메시지 `0`, 기존 조직 활성 기기 `5`, 비활성 기기 `0` 확인
- 공개·보안 경계:
  - 테스트 결과와 판단 근거만 공개 작업 기록에 남김
  - Supabase 스키마·정책 마이그레이션과 테스트 SQL 원본은 `.gitignore`의 `supabase/`에 유지해 공개 GitHub에 포함하지 않음
  - 현재는 SQL Editor에서 한 번 실행하면 모든 사례를 자동 검사하는 단계이며, 커밋마다 자동 실행되는 GitHub Actions CI는 아직 아님
- 다음 권장 단계:
  - 운영 데이터에 영향을 주지 않는 별도 Supabase 테스트 프로젝트 마련
  - 비공개 CI 자격증명을 구성한 뒤 동일 회귀 테스트를 GitHub Actions에서 커밋마다 실행
  - 제품 기능은 메시지 전달·읽음 상태와 오프라인 재접속 순서로 확장

### 2026-09-01 — 테세라 전달·읽음 상태와 재접속 동기화

- 제품 정의:
  - `전송됨`: Supabase가 암호문 저장을 수락한 상태
  - `전달됨`: 상대 기기가 암호문을 받아 복호화한 상태
  - `읽음`: 상대가 해당 대화 화면을 보고 있는 상태
- Supabase 변경:
  - `tessera_messages`에 최소 메타데이터인 `delivered_at`, `read_at` 추가
  - 읽음 시각이 전달 시각보다 빠를 수 없도록 데이터베이스 제약 추가
  - AAL2 수신자와 활성 수신 기기만 호출 가능한 `acknowledge_tessera_messages(...)` SECURITY DEFINER 함수 추가
  - 로그인 사용자의 메시지 테이블 직접 UPDATE 권한은 계속 차단
  - Realtime UPDATE가 전체 새 행을 전달하도록 `REPLICA IDENTITY FULL` 적용
- 클라이언트 변경:
  - 내가 보낸 메시지 아래에 `전송됨 → 전달됨 → 읽음` 상태 표시
  - 새 암호문을 실제 복호화한 뒤에만 전달 확인 기록
  - 테세라 화면이 보이는 동안에만 읽음 확인 기록
  - Realtime INSERT뿐 아니라 수신확인 UPDATE도 구독해 발신자 화면에 즉시 반영
  - 브라우저가 오프라인이면 전송을 막되 작성 중인 내용은 유지
  - 다시 온라인이 되어 Realtime 구독이 복구되면 최근 대화를 재조회해 누락된 암호문과 수신 상태 동기화
- 서버 검증:
  - 전달·읽음 컬럼, 수신확인 함수, 직접 UPDATE 차단, SECURITY DEFINER, 전체 행 Realtime 설정 등 6개 항목 모두 `true`
  - 확장 보안 회귀 테스트에서 AAL2 수신자의 전달·읽음 기록 허용과 AAL1 수신확인 차단 확인
  - 기존 정상 전송·AAL1 전송 차단·다른 조직 차단·비당사자 조회 차단·폐기 기기 차단도 계속 통과
  - 최종 `ROLLBACK` 결과 확인으로 시험 메시지와 기기 상태 변경 미잔존
- 현재 한계:
  - 수신확인 시각은 메시지 내용이 아닌 메타데이터지만 대화 활동 시점을 드러낼 수 있음
  - 최근 100개 메시지만 재동기화하며 페이지네이션은 아직 없음
  - 오프라인 중 새 메시지를 저장했다가 자동 전송하는 발신 큐는 아직 없음
- 배포·화면 검증:
  - 기능 커밋 `4a7f8d7`을 GitHub `main`에 push
  - GitHub Pages build·deploy 성공
  - NCP 자동 배포 상태 커밋 `4a7f8d7`, 타이머 `active`, 서버 내부 `/app/` HTTP 200 확인
  - 로그인된 GitHub Pages에서 `ONLINE / SYNCED`, 기존 대화 재조회, 발신 메시지의 `전송됨` 표시 확인
  - 390 × 844 모바일에서 대화 화면과 수신 상태가 렌더링되고 수평 overflow 없음 확인
  - 사용자 계정으로 임의 시험 메시지는 발송하지 않았으며 `전달됨 → 읽음` 상태 전환은 롤백되는 서버 회귀 테스트로 검증

### 2026-09-01 — 테세라 최근 대화·안 읽음 목록

- 작업 목적:
  - 조직 명단처럼 보이던 왼쪽 목록을 일반 메신저에 가까운 대화 목록으로 개선
  - 최근 활동이 있는 대화를 위로 올리고, 확인하지 않은 메시지를 한눈에 파악
- Supabase 변경:
  - `list_tessera_conversation_summaries(uuid)` SECURITY DEFINER 함수 추가
  - AAL2 조직 구성원에게 본인이 참여한 대화의 상대 사용자 ID, 최근 메시지 시각, 안 읽은 개수만 반환
  - 메시지 평문·암호문·미리보기는 요약 결과에 포함하지 않음
- 클라이언트 변경:
  - 최근 메시지 시각 기준 대화 상대 정렬
  - 오늘 대화는 시·분, 이전 대화는 월·일 표시
  - 안 읽은 메시지가 있으면 숫자 배지 표시하고 99개 초과는 `99+`로 제한
  - 대화를 읽거나 새 암호문을 수신·전송하거나 재접속할 때 요약 자동 갱신
  - 메시지가 없는 조직 구성원은 `새 대화`로 계속 표시
- 보안 회귀 테스트:
  - 기존 안 읽은 메시지 개수를 기준값으로 측정한 뒤 시험 메시지 삽입 후 정확히 1 증가하는지 검증
  - AAL2 요약 조회와 기존 전송·수신확인·조직·비당사자·폐기 기기 차단 테스트 모두 통과
  - 전체 트랜잭션 `ROLLBACK`으로 시험 메시지와 상태 변경 미잔존 확인
- 실제 화면에서 발견·수정한 기기 경계:
  - 최초 요약은 같은 사용자의 과거 다른 기기로 온 메시지까지 안 읽음으로 집계해 현재 기기에서 복호화할 수 없는 항목이 보였음
  - 요약 RPC에 현재 기기 ID를 추가하고 AAL2 사용자 소유의 활성 기기인지 서버에서 검증
  - 메시지 조회도 현재 기기가 보냈거나 현재 기기로 받은 암호문만 반환하도록 제한
  - 현재 기기에서 실제로 읽을 수 있는 메시지만 최근 시각·안 읽음 계산에 포함
  - 배포 후 이전 JavaScript가 브라우저 캐시에 남아 구형 RPC 시그니처를 호출한 문제를 확인하고 자산 버전을 갱신
- 최종 배포·화면 검증:
  - 자산 갱신 커밋 `8af5943`을 GitHub `main`에 push
  - GitHub Pages 실행 `33411522861` build·deploy 성공
  - NCP 저장소 HEAD와 배포 상태가 모두 `8af5943`으로 일치하고 자동 배포 타이머 `active`, 서버 내부 `/app/` HTTP 200 확인
  - 로그인된 GitHub Pages에서 `ONLINE / SYNCED`, 최근 대화 날짜, 현재 기기의 정상 복호화 메시지만 표시되는 것을 확인
  - 구형 RPC 호출 오류 `PGRST202`와 과거 다른 기기의 복호화 불가 메시지가 더 이상 나타나지 않음
  - 390 × 844 모바일에서 대화 화면이 유지되고 수평 overflow가 없음을 확인
  - 브라우저 로그에서 앱 자체 오류 없음(확장 프로그램 로그는 검증 대상에서 제외)

### 2026-09-01 — 테세라 다중 기기 수신 개선

- 발견 배경:
  - Edge의 멤버 계정이 관리자에게 보낸 메시지는 `전송됨`이었지만 Chrome 관리자 화면에는 나타나지 않음
  - 발신 화면이 선택한 관리자 기기 지문 `F61F:C389:208F:21DB`와 Chrome 관리자의 현재 기기 지문 `F691:AC16:CEAD:41C6`이 서로 다른 것을 확인
  - 기존 MVP가 사람별 활성 기기 중 한 대만 골라 암호화한 것이 직접 원인
- 데이터 구조 개선:
  - `tessera_messages.message_group_id`를 추가해 한 논리 메시지와 여러 기기별 암호문을 연결
  - 기존 메시지는 자신의 메시지 ID를 그룹 ID로 사용하도록 안전하게 변환
  - 같은 논리 메시지·같은 수신 기기 조합의 중복 암호문은 데이터베이스 고유 인덱스로 차단
- 클라이언트 개선:
  - 대화 상대의 모든 활성 기기 공개키로 같은 평문을 각각 ECDH/HKDF/AES-GCM 암호화
  - Supabase에는 기기별로 서로 다른 IV와 암호문을 저장하고 평문은 계속 전송하지 않음
  - 발신자 화면에서는 같은 그룹의 기기별 암호문을 하나의 메시지로 중복 제거
  - 전달·읽음 상태는 한 기기 이상에서 확인되면 낮은 상태로 되돌아가지 않고 승격
  - 수신 브라우저는 현재 기기를 대상으로 한 암호문만 실시간 처리해 다른 기기용 암호문의 복호화 오류를 방지
  - 대화 헤더에 상대방의 암호화 대상 활성 기기 수를 표시
- 서버 검증:
  - 그룹 ID 필수 컬럼과 기기별 중복 차단 인덱스가 모두 적용된 것을 확인
  - 실제 조직의 활성 기기 2대를 대상으로 한 AAL2 봉투 저장 허용 확인
  - 동일 그룹·동일 수신 기기의 중복 봉투 차단 확인
  - 전체 시험은 트랜잭션 `ROLLBACK`으로 종료해 시험 메시지가 남지 않음
- 배포 및 남은 사용자 검증:
  - 기능 커밋 `d6c19c9`을 GitHub `main`에 push
  - GitHub Pages 실행 `33466293778` build·deploy 성공
  - 배포 HTML에서 `260901-4` JavaScript 자산과 다중 기기 안내 문구 확인
  - 배포 JavaScript에서 기기별 `message_group_id` 봉투 생성과 일괄 저장 코드 확인
  - NCP 저장소 HEAD와 배포 상태가 모두 `d6c19c9`, 자동 배포 타이머 `active`, `/app/` HTTP 200 확인
  - 기존 `하이` 메시지는 과거 단일 기기 암호문이므로 Chrome에 소급 전달되지 않음
  - Edge와 Chrome을 모두 새로고침한 뒤 새 메시지로 실시간 수신·전달·읽음 상태를 최종 확인할 예정

### 2026-09-01 — 트러스트 랩 데스크톱 가독성 개선

- 사용자 확인:
  - 다중 기기 새 메시지가 Chrome 관리자 화면에 도착하는 것을 확인
  - PC 화면에서 보조 안내 글씨가 지나치게 작고 어두워 읽기 어렵다는 피드백 확인
- 조정 원칙:
  - 스위스 편집 디자인의 큰 여백·대비·모노스페이스 메타 표현은 유지
  - 기능 설명과 상태 정보는 장식이 아니라 사용에 필요한 정보이므로 최소 글자 크기와 대비를 상향
- 개선 범위:
  - 왼쪽 제품 메뉴의 번호·제품명·설명, 로그인 사용자·세션 종료·프로토타입 안내
  - 대화 목록의 상태·별명·VAULT ID·최근 시각·목록 안내
  - 메시지 작성자·시간·본문·전송/전달/읽음 상태
  - 전송 결과, 암호화 경계 안내, 입력 placeholder와 작성창 하단 설명
  - 밝은 배경과 어두운 배경에 각각 맞는 골드·보조 텍스트 색상 토큰 분리
- 검증:
  - `app/app.js`, `app/identity.js` JavaScript 문법 검사 통과
  - CSS diff 공백 오류 없음
  - 핵심 색상 대비: 어두운 배경 골드 `7.07:1`, 밝은 배경 골드 `5.34:1`, 아이보리 본문 `15.49:1`
  - 대화 목록·메시지 상태·작성창 안내의 PC 기준 글자 크기를 약 `11~16px` 범위로 상향
- 배포:
  - 기능 커밋 `81cfdc7`을 GitHub `main`에 반영
  - GitHub Pages 실행 `33469250849` 성공, 공개 페이지에서 `app.css?v=260901-3` 제공 확인
  - NCP 자동 배포 HEAD `81cfdc7`, 배포 타이머 `active`, `/app/` HTTP 200 및 동일 CSS 버전 확인

### 2026-09-01 — 브랜드명 교정 및 한글 대형 타이틀 개선

- 브랜드명:
  - 잘못 표기된 `테세러`를 전체 공개 페이지·트러스트 랩·다국어 사전에서 `테세라`로 통일
- 대형 제품 타이틀:
  - 한국어에서는 `더 볼트 룸`, `테세라`, `디지털 볼트`를 음절 단위로 나누지 않고 가로 한 줄로 표시
  - 한국어 제목 전용 크기·행간·자간을 적용하여 데스크톱과 모바일에서 겹침을 방지
  - 영어 제목은 기존의 편집적인 다단 줄바꿈을 유지
- 캐시 갱신:
  - 메인 스타일 `styles.css?v=260901-1`
  - 공통 번역 사전 `i18n.js?v=260901-1`
- 검증·배포:
  - 전체 소스에서 과거 오표기 제거 여부, JavaScript 문법과 Git diff 오류 검사 통과
  - 기능 커밋 `4d10515`을 GitHub `main`에 반영
  - GitHub Pages 실행 `33469807348` 성공, 공개 HTML에서 `data-ko="테세라"`와 새 캐시 버전 확인
  - NCP 자동 배포 HEAD `4d10515`, 루트 HTTP 200 및 동일 타이틀 확인

### 2026-09-01 — 디지털 볼트 영구 암호문 보관 기반

- 이번 단계의 범위:
  - 테세라 파일 첨부 공유에 앞서, 소유자가 파일을 암호화해 서버에 보관하고 다시 복호화할 수 있는 기반을 우선 구현
  - 공유·만료·상대방 기기 키 전달은 다음 단계로 분리
- 브라우저 암호화:
  - 파일마다 AES-256-GCM 키와 IV를 새로 생성
  - 파일 본문뿐 아니라 파일명·MIME 타입·원본 크기도 별도 암호화
  - 파일 키는 현재 기기의 ECDH/HKDF 키로 다시 암호화한 기기 키 봉투로 저장
  - 서버에서 받은 암호문의 SHA-256을 비교한 후에만 복호화 다운로드
- Supabase:
  - 비공개 `vault-objects` Storage 버킷 생성, 최대 암호문 크기 약 10MB
  - `vault_files`, `vault_file_keys` 테이블과 소유자·AAL2·조직·활성 기기 검증 RPC 구성
  - 오브젝트 업로드·다운로드·삭제 정책을 서버 함수로 제한
  - 파일 접근키 폐기 시 기기 키 봉투를 삭제하고 서버 암호문은 유지
- 클라이언트:
  - Supabase 로그인에서는 암호문 업로드와 메타데이터 등록을 연결
  - 새로고침 후 현재 기기 키로 파일 목록과 파일명을 복원
  - 다운로드 시 암호문 무결성 검증 후 원래 파일명·형식으로 복호화
  - 로컬 프리뷰의 기존 메모리 전용 암호화 시연은 유지
- 검증:
  - 서버 구조 7개 항목(두 테이블, 비공개 버킷, 세 RPC, Storage 정책) 모두 `true`
  - 회귀 테스트에서 AAL2 소유자의 등록·목록·폐기 허용과 AAL1 등록 차단을 확인
  - 테스트는 마지막에 `ROLLBACK`되어 시험 파일 메타데이터와 키 봉투가 남지 않음
  - JavaScript 문법 검사와 Git diff 오류 검사 통과
- 배포:
  - 기능 커밋 `74c236d`을 GitHub `main`에 반영
  - GitHub Pages 실행 `33473631257` build·deploy 성공
  - 공개 `/app/`에서 로그인 게이트와 새 `app.js?v=260901-5` 자산 로딩 확인
  - NCP 자동 배포 HEAD `74c236d`, `/app/` HTTP 200 및 동일 JavaScript 버전 확인
- 남은 실제 사용자 검증:
  - 배포 앱에 AAL2로 로그인해 작은 파일 1개 업로드
  - 새로고침 후 파일명 복원과 복호화 다운로드 확인
  - 접근키 폐기 후 동일 파일이 다운로드 불가 상태로 바뀌는지 확인

### 2026-09-01 — 디지털 볼트 소유자 경로 확인 및 테세라 파일 공유 준비

- 사용자 실기기 확인:
  - `Alien Mind.html`을 디지털 볼트에 업로드한 뒤 원래 파일명이 복원되는 것을 확인
  - `복호화 다운로드`로 원본 파일이 내려받아지는 것을 확인
  - 이 결과로 브라우저 암호화 → 서버 암호문 저장 → 기기 키로 파일 키 복원 → 원본 다운로드의 소유자 경로를 확인
- 테세라 파일 첨부 클라이언트:
  - 대화 작성창에 `파일 첨부` 동작 추가
  - 파일 본문·파일명·형식·크기를 브라우저에서 암호화하고 서버에는 암호문만 저장
  - 상대방의 모든 활성 기기 공개키로 파일 키 봉투를 각각 생성
  - 테세라 메시지에는 암호화된 파일 참조만 전달하고, 수신자는 디지털 볼트에서 파일을 복호화
  - 받은 파일에는 다운로드만 표시하고 소유자에게만 `접근키 폐기` 권한을 표시
- Supabase 비공개 서버 변경 준비:
  - `vault_file_keys.wrapping_device_id`와 `share_vault_file` RPC 추가
  - 공유받은 사용자의 현재 기기만 파일 목록·Storage 암호문에 접근하도록 `list_vault_files` 확장
  - 소유자 폐기 시 모든 수신 기기 키 봉투를 삭제하도록 `revoke_vault_file` 강화
  - 수신자 접근, AAL1 차단, 소유자 폐기를 검증하는 롤백형 회귀 테스트 작성
- 검증:
  - `app/app.js`, `app/identity.js` JavaScript 문법 검사 통과
  - 송신자·수신자 ECDH/HKDF 파일 키 봉투가 같은 AES 키를 만드는 대칭성 테스트 통과
  - Git diff 공백 오류 없음
- 아직 남은 작업:
  - 공개 프론트엔드 배포
  - 관리자·멤버 두 브라우저에서 첨부 수신, 디지털 볼트 다운로드, 소유자 폐기 후 차단을 실제 확인
  - 파일 공유 RPC 성공 뒤 메시지 저장이 실패하는 드문 부분 성공 상황은 후속 재시도/상태 모델에서 보완
- Supabase 적용 결과:
  - 파일 공유 마이그레이션 적용 성공
  - 수신 기기 봉투·AAL2 강제·소유자 폐기 차단을 확인하는 회귀 테스트 성공 후 전체 롤백
  - `wrapping_device_id`, 공유 RPC, 공유 목록 반환값, 수신 키 일괄 폐기 정의를 조회한 결과 4개 항목 모두 `true`
- 배포:
  - 기능 커밋 `b4b15a8`, 검증 기록 포함 커밋 `69e8ad7`을 GitHub `main`에 반영
  - GitHub Pages 실행 `33505463862` build·deploy 성공
  - 공개 `/app/`에서 `app.css?v=260901-4`, `identity.js?v=260901-6`, `app.js?v=260901-6` 제공 확인
  - NCP 자동 배포 상태 커밋 `69e8ad7`, 타이머 `active`, 서버 내부 `/app/`에서 동일 자산 버전 확인

### 2026-09-01 — 모바일 앱형 UX·PWA·패스키 준비

- 제품 방향:
  - 테세라와 디지털 볼트를 서로 분리된 두 앱이 아니라 하나의 `THE VAULT` 설치형 PWA 안에서 전환하는 구조로 결정
  - 브랜드·신원·기기 보안 경계는 공유하고, 실제 사용 흐름은 서비스별 모바일 화면으로 분리
- 모바일 UX:
  - 앱 전용 상단 바와 테세라·디지털 볼트·기록·설정 하단 탭 추가
  - 디지털 볼트의 텍스트, 일반 파일, 사진·영상, 카메라 추가 메뉴를 모바일 바텀시트로 구성
  - 모바일 로그인 화면은 대형 데스크톱 타이포그래피 대신 즉시 입력 가능한 앱형 화면으로 재설계
  - VAULT ID, 기기 상태, 설치, 패스키, 로그아웃을 한 설정 화면에 통합
- PWA:
  - manifest, 독립 실행 모드, 앱 아이콘, 서비스 워커와 오프라인 앱 셸 추가
  - 일부 정적 자산이 일시적으로 실패해도 서비스 워커 설치 전체가 중단되지 않도록 캐시 설치를 내결함성 있게 구성
  - 숨김 파일 입력과 접근성 라벨이 공통 사이트 CSS 없이도 노출되지 않도록 앱 자체 스타일 보강
- 패스키:
  - 지원 기기에서 지문·Face ID·기기 PIN을 사용할 수 있는 WebAuthn 패스키 로그인·등록 UI 준비
  - 생체정보 원본은 THE VAULT나 Supabase로 보내지 않고 기기에서 로그인 서명 승인에만 사용
  - 패스키 로그인 뒤에도 중요한 작업은 기존 TOTP AAL2를 통과하도록 현재 보안 경계 유지
  - 실제 등록 활성화는 고정 HTTPS 도메인과 Supabase Passkey 서버 설정 확정 후 진행
- 검증:
  - 390 × 844 모바일에서 로그인, 테세라, 디지털 볼트, 추가 메뉴, 설정 화면을 실제 브라우저로 확인
  - 1440 × 900에서 기존 데스크톱 작업영역 DOM과 주요 기능이 유지되는지 회귀 확인
  - JavaScript 문법, manifest JSON, Git diff 공백 오류 검사 통과
- 배포:
  - 기능 커밋 `09976aa`를 GitHub `main`에 반영
  - GitHub Pages 실행 `33514412327` 성공, 공개 `/app/`의 새 manifest와 모바일 UI 자산 확인
  - NCP `/app/`에서도 동일 manifest와 `app.css?v=260901-5`, `identity.js?v=260901-7`, `app.js?v=260901-7` 확인

### 2026-09-01 — Mobile Friendly V2 브랜치 분리 및 생활형 금고 UX

- 브랜치 전략:
  - 공개 기준 버전을 보존하고 `codex/mobile-friendly-v2` 실험 브랜치를 생성
  - 인증, 조직 권한, 메시지와 파일 암호화 구조는 유지하고 프론트 정보구조와 표현만 우선 개선
- 디지털 금고 서비스:
  - 모바일 첫 화면을 `패스워드`, `내면 일기`, `잠긴 추억`, `파일 보관`의 목적 중심 구조로 변경
  - 패스워드는 안전한 값 생성, 암호화 보관, 앱 내부 열기, 보기·숨기기, 복사 기능 제공
  - 내면 일기는 제목과 본문을 암호화 항목으로 보관하고 앱 내부에서 다시 읽는 흐름 제공
  - 잠긴 추억은 사진·영상 선택 후 공유 기능 없이 보관하고 접근키 폐기 전 복구 불가 경고 표시
  - 기존 AES-GCM 파일 암호화와 기기별 키 봉투를 재사용하여 새 평문 저장소를 만들지 않음
- 사용성 원칙:
  - 기술 용어보다 사용 목적과 다음 행동을 먼저 표시
  - `지울 수 없음` 대신 사용자 삭제권을 유지하면서 실수 방지와 재확인을 제공하는 `잠긴 추억`으로 정의
  - 복호화 상세 화면을 닫을 때 패스워드와 일기 평문 DOM, 메모리 참조와 미디어 URL을 정리
- 학습 문서:
  - `브랜치 작업 매뉴얼.MD`에 브랜치 확인, 전환, Mac·Windows 연속 작업, push·pull, 공개 배포의 차이를 정리
- 검증:
  - 390 × 844 모바일에서 목적 중심 금고 홈, 패스워드 생성·보관·열기, 내면 일기 보관·다시 읽기를 실제 브라우저로 확인
  - 패스워드 상세는 기본 마스킹, 보기·숨기기와 클립보드 복사 경고를 적용
  - 1440 × 900에서 기존 디지털 볼트 업로드·경계 검사 화면이 유지되는지 확인
  - JavaScript 문법, PWA manifest JSON, Git diff 공백 오류 검사 통과

### 2026-09-02 — Mobile Friendly V2 독립 미리보기 준비

- 기존 `main` 운영 릴리스와 V2 파일이 서로 덮어쓰지 않도록 NCP 웹 루트를 분리
- V2 전용 서버 worktree와 `/var/www/the-vault-previews/mobile-v2/current` 릴리스 경로 사용
- Nginx `/preview/mobile-v2/` 경로만 V2 웹 루트로 연결하도록 구성
- 운영 `main`의 1분 자동 배포와 V2 수동 미리보기가 서로 독립적으로 유지되도록 설계
- 배포·검증:
  - NCP 서버 worktree를 V2 커밋 `f7e136e`로 생성하고 별도 릴리스 발행
  - `http://110.165.18.205/preview/mobile-v2/app/`에서 HTML, CSS, JavaScript, manifest 모두 HTTP 200 확인
  - `/preview/mobile-v2`는 trailing slash 주소로 301 정규화되고 Nginx 설정 검사·reload 성공
  - 현재 IP HTTP 주소는 시각 비교용이며 PWA 설치·패스키·실사용 Web Crypto는 HTTPS 도메인 적용 후 검증해야 함
