# THE VAULT 백엔드·서버 초보자 학습 가이드

> 대상: 프론트엔드 작업 이후의 서버·백엔드 개념이 처음인 사용자  
> 작성일: 2026-08-28  
> 범위: GitHub Pages, Supabase, NCP 서버까지 진행한 내용과 다음 단계

## 1. 가장 먼저 이해할 전체 구조

현재 THE VAULT는 다음과 같은 구조로 만들어져 있다.

```text
사용자 브라우저
      │
      ├─ 화면 요청 ──→ GitHub Pages
      │                랜딩페이지·제품 페이지·Trust Lab 화면
      │
      └─ 로그인·데이터 요청 ──→ Supabase
                                회원·권한·DB·서버 함수
```

새로 생성한 NCP 서버는 아직 웹사이트 운영에 연결하기 전 단계다.

```text
현재 구조: GitHub Pages + Supabase
다음 구조: NCP 프론트 서버 + Supabase
```

핵심은 화면과 백엔드를 서로 다른 곳에서 운영할 수 있다는 것이다.

## 2. 프론트엔드는 무엇인가?

프론트엔드는 사용자가 브라우저에서 보는 부분이다.

THE VAULT 프로젝트에서는 다음 파일과 디렉터리가 프론트엔드에 해당한다.

- `index.html`: 화면의 구조
- `styles.css`: 색상, 글자, 여백과 배치
- `script.js`: 스크롤과 애니메이션
- `app/`: VAULT TRUST LAB 화면과 브라우저 암호화 기능
- `tessera/`: 테세라 제품 페이지
- `digital-vault/`: 디지털 볼트 제품 페이지

프론트엔드는 은행의 객장이나 금고의 조작 화면과 비슷하다. 사용자가 버튼을 누르고 정보를 입력하는 곳이지만, 중요한 데이터가 실제로 처리되는 장소는 별도로 필요하다.

## 3. GitHub는 무슨 역할인가?

GitHub는 프로그램을 실행하는 주 서버라기보다 코드의 원본 보관소다.

현재 공개 저장소:

```text
groot512/the-vault-landing
```

GitHub에는 다음 정보가 기록된다.

- 현재 코드
- 수정 이력
- 누가 무엇을 변경했는지
- 맥북과 집 PC가 공유할 프로젝트 원본
- 이전 버전으로 돌아갈 수 있는 기록

GitHub는 건물 자체가 아니라 최신 설계도와 공사 기록을 보관하는 중앙 문서실과 비슷하다.

GitHub Pages 기능을 사용하면 GitHub에 저장한 정적 웹사이트를 인터넷에 공개할 수도 있다. 현재 THE VAULT 페이지는 이 방식으로 배포되어 있다.

## 4. Supabase는 무엇인가?

Supabase는 현재 THE VAULT의 관리형 백엔드 서버다.

관리형이라는 말은 서버 설치, 데이터베이스 실행, 인증 시스템 운영 같은 어려운 일을 Supabase가 대신 관리한다는 뜻이다.

### 4.1 사용자 인증

현재 Supabase Auth가 담당하는 기능은 다음과 같다.

- VAULT ID와 비밀번호 로그인
- 최초 로그인 시 비밀번호 변경
- 인증 앱을 이용한 TOTP 6자리 인증
- AAL2 수준의 2단계 인증 상태 확인
- 로그인 세션 유지와 로그아웃

### 4.2 데이터베이스

현재 사용하는 주요 테이블은 다음과 같다.

- `profiles`: 사용자의 VAULT ID
- `organizations`: 조직
- `memberships`: 사용자가 어느 조직에 속했는지와 역할
- `devices`: 등록된 기기와 공개키 정보
- `recovery_credentials`: 복구키 검증 정보

복구키의 12개 단어 원문은 서버에 저장하지 않는다. 원문으로부터 만든 검증값만 저장한다.

### 4.3 RLS 권한 통제

RLS는 데이터베이스에 적용하는 출입 규칙이다.

- 일반 사용자는 자기 조직에서 허용된 정보만 조회
- 관리자는 조직 구성원을 관리
- 다른 조직의 정보에는 접근 불가
- 중요한 기능은 TOTP 인증을 완료한 세션만 사용

RLS는 데이터베이스 문 앞에 배치된 자동 경비원과 비슷하다.

### 4.4 Edge Functions

Edge Function은 브라우저에 맡기면 위험한 관리자 작업을 처리하는 서버 프로그램이다.

현재 구현한 기능은 다음과 같다.

- 임시 VAULT 계정 발급
- 임시 비밀번호 생성
- 사용자를 조직 구성원으로 등록
- 구성원 접근 정지와 복구
- 호출자의 관리자 권한 확인

중요한 관리자 비밀키는 브라우저나 GitHub에 넣지 않고 Supabase 서버 내부에서만 사용한다.

## 5. 현재 회원 발급과 최초 로그인 과정

현재는 사용자가 이메일로 직접 가입하는 일반적인 회원가입 방식이 아니다.

```text
조직 관리자
   ↓
임시 VAULT ID와 임시 비밀번호 발급
   ↓
사용자에게 안전한 오프라인 방식으로 전달
   ↓
사용자 최초 로그인
   ↓
자신의 비밀번호로 변경
   ↓
인증 앱 TOTP 등록
   ↓
12개 단어 복구키 발급
   ↓
기기 키 생성
   ↓
Trust Lab 입장
```

실명, 실제 이메일, 휴대전화 번호는 수집하지 않는 방향으로 설계했다.

관리자가 임시 계정을 발급하고, 신규 사용자가 최초 로그인하여 비밀번호 변경·TOTP 등록·복구키 확인 후 `MEMBER` 권한으로 입장하는 과정까지 실제로 확인했다.

구성원 접근 정지·복구 기능은 구현되어 있다. 특정 테스트 계정을 대상으로 한 정지 → 로그인 차단 → 복구 → 재로그인의 전체 검증은 별도의 검증 단계로 남아 있다.

## 6. 메시지와 파일은 Supabase에 저장되고 있는가?

아직 저장되지 않는다.

현재 Trust Lab의 메시지와 파일 기능은 브라우저 암호화 원리를 증명하는 기술 시연이다.

### 메시지

- 브라우저에서 암호화
- 브라우저에서 다시 복호화
- 서버로 실제 전송하지 않음

### 파일

- 파일별 암호화 키 생성
- 브라우저에서 AES-GCM 방식으로 암호화
- 키가 있을 때 복호화
- 키를 폐기하면 접근할 수 없는 상태 시연
- 암호화된 파일을 서버에 영구 저장하지 않음

따라서 현재 구현은 암호화가 실제로 동작한다는 것을 보여주는 MVP다. 완성된 메신저나 디지털 금고는 아니다.

## 7. NCP는 무엇인가?

NCP는 네이버클라우드플랫폼이다.

우리가 생성한 NCP 서버는 인터넷에 연결된 임대 컴퓨터 한 대라고 이해하면 된다.

현재 서버 정보:

- 공인 IP: NCP 콘솔과 로컬 SSH 설정에서만 관리
- 운영체제: Ubuntu 24.04
- CPU: 1 vCPU
- 메모리: 약 1GB
- 디스크: 10GB
- 관리자 계정: `root`

Supabase처럼 완성된 백엔드 서비스가 아니다. 빈 컴퓨터를 빌린 것에 가까우므로 웹서버와 보안 설정을 직접 설치해야 한다.

## 8. SSH와 PEM은 무엇인가?

SSH는 맥북에서 멀리 떨어진 NCP 서버의 터미널에 접속하는 방법이다.

```bash
ssh root@NCP_공인_IP
```

터미널 표시를 보면 현재 위치를 구분할 수 있다.

```text
groot5@groot5 ~ %
```

위 표시는 맥북 로컬 터미널이다.

```text
root@groot512:~#
```

위 표시는 NCP 서버 터미널이다.

명령을 실행하기 전에는 프롬프트 왼쪽을 확인하는 습관이 중요하다.

PEM 파일은 NCP 서버를 만들 때 받은 인증키다.

```text
~/.ssh/ncp/{NCP 인증키}.pem
```

처음에는 NCP가 자동 생성한 관리자 비밀번호를 확인하는 데 사용했다. 이후 해당 PEM의 공개키를 NCP 서버의 `/root/.ssh/authorized_keys`에 등록했다.

이제 비밀번호를 전달하지 않고도 PEM을 이용해 서버를 관리할 수 있다.

## 9. NCP에 Supabase 전체를 설치하지 않은 이유

Supabase 전체를 자체 운영하면 다음 프로그램들이 동시에 실행된다.

- PostgreSQL
- 인증 서버
- REST API
- 실시간 통신
- 파일 저장소
- Edge Functions
- 관리 도구
- Docker 컨테이너

현재 NCP 서버는 메모리 1GB, 디스크 10GB이므로 Supabase 전체를 안정적으로 실행하기에는 작다.

그러나 프론트엔드 웹서버만 운영하기에는 충분하다.

따라서 현재 권장 구조는 다음과 같다.

```text
NCP
 └─ 랜딩페이지·제품 페이지·Trust Lab 프론트엔드

Supabase
 └─ 로그인·TOTP·DB·RLS·Edge Functions

GitHub
 └─ 코드 원본·변경 이력·배포 자동화
```

이 방식은 임시방편이 아니라 실제 서비스에서도 자주 사용하는 프론트엔드와 백엔드 분리 구조다.

## 10. 현재 완료된 작업

- 브랜드 랜딩페이지
- 테세라 제품 페이지
- 디지털 볼트 제품 페이지
- Trust Lab 암호화 기술 시연
- GitHub 공개 저장소
- GitHub Pages 배포
- Supabase 프로젝트 연결
- VAULT ID 로그인
- 최초 로그인 비밀번호 변경
- TOTP 인증
- 복구키 발급
- 조직과 관리자·멤버 권한
- 관리자 임시 계정 발급
- 구성원 접근 관리 기능
- NCP 서버 생성
- NCP `root` 접속
- PEM 공개키 등록
- PEM 기반 비밀번호 없는 SSH 접속
- NCP 서버 사양 점검
- NCP Nginx 웹서버 설치
- 웹에 필요한 파일만 배포하는 릴리스 스크립트 작성
- NCP 내부에서 홈·제품 페이지·Trust Lab·이미지 HTTP 200 확인
- 보안 응답 헤더와 Supabase 연결 허용 범위 적용

## 11. 아직 남은 작업

- NCP ACG에서 외부 HTTP 80과 HTTPS 443 포트 허용
- 도메인을 NCP 공인 IP에 연결
- HTTPS 적용
- NCP가 GitHub의 최신 원본을 가져오는 운영 배포 흐름 확정
- Supabase Edge Function의 허용 Origin에 새 도메인 추가
- NCP 환경에서 로그인과 TOTP 재검증
- GitHub 수정 사항을 NCP에 자동 배포하는 과정
- 구성원 접근 정지·복구 전체 검증
- 암호화 파일의 실제 영구 저장
- 실제 테세라 사용자 간 메시지 전달
- 서버 백업과 장애 복구

현재 NCP 내부에서는 프론트엔드가 정상 실행되지만, 외부에서 공인 IP의 80번 포트로 접속하면 시간 초과가 발생한다. 이는 Nginx 문제가 아니라 NCP ACG에서 80번 포트를 아직 허용하지 않았기 때문이다. 도메인과 HTTPS가 준비되기 전까지 GitHub Pages를 안전한 운영본으로 유지한다.

## 12. 다음 배포 작업에서 배우게 될 내용

다음 단계는 NCP에 프론트엔드를 올리는 것이다.

진행하면서 다음 개념을 실제로 배우게 된다.

1. 웹서버가 HTML 파일을 브라우저에 전달하는 방법
2. IP 주소와 도메인의 차이
3. HTTPS 인증서가 필요한 이유
4. GitHub 코드가 서버에 배포되는 과정
5. 프론트엔드가 Supabase API를 호출하는 과정
6. 서버가 재시작되어도 웹사이트를 유지하는 방법
7. 방화벽에서 80번·443번 포트를 여는 이유

배포 흐름은 다음과 같다.

```text
GitHub
   ↓ 코드 배포
NCP의 웹서버
   ↓ HTTPS 페이지 제공
사용자 브라우저
   ↓ 로그인·DB 요청
Supabase
```

## 13. 초보자가 꼭 구분해야 할 용어

| 용어 | 쉬운 의미 | THE VAULT에서의 역할 |
|---|---|---|
| 프론트엔드 | 사용자가 보는 화면 | 랜딩페이지, 제품 페이지, Trust Lab |
| 백엔드 | 화면 뒤에서 데이터를 처리하는 프로그램 | Supabase |
| 데이터베이스 | 구조화된 정보를 보관하는 장소 | 사용자, 조직, 권한, 기기 정보 |
| 서버 | 인터넷에 연결되어 계속 실행되는 컴퓨터 | NCP Ubuntu 서버 |
| GitHub | 코드와 변경 이력을 보관하는 장소 | 프로젝트의 원본 저장소 |
| 배포 | 만든 코드를 실제 서버에 올리는 과정 | GitHub Pages 또는 NCP에 게시 |
| API | 프론트엔드가 백엔드에 요청하는 통로 | 로그인, 계정 발급, 구성원 관리 |
| IP 주소 | 인터넷상의 컴퓨터 주소 | NCP 콘솔에서 확인하는 공인 IP |
| 도메인 | 사람이 기억하기 쉬운 인터넷 주소 | 앞으로 연결할 THE VAULT 주소 |
| HTTPS | 브라우저와 서버 사이의 암호화 연결 | 로그인과 Web Crypto 사용에 필요 |
| SSH | 원격 서버 터미널에 접속하는 방법 | 맥북에서 NCP 관리 |
| PEM | SSH 접근에 사용하는 개인 인증키 | 비밀번호 없이 NCP 접속 |
| root | Linux 서버의 최고 관리자 | NCP 설치·설정 권한 |
| RLS | 데이터베이스 행 단위 출입 규칙 | 조직과 사용자별 데이터 격리 |
| TOTP | 인증 앱에서 생성되는 6자리 코드 | 2단계 인증 |

## 14. 보안상 반드시 기억할 것

- PEM 개인키를 메신저, 이메일, GitHub에 올리지 않는다.
- Supabase `service_role` 키를 프론트엔드 코드에 넣지 않는다.
- Supabase Publishable Key는 브라우저용이지만 RLS가 반드시 적용되어야 한다.
- 비밀번호나 복구키 원문을 작업기록에 남기지 않는다.
- 터미널 명령을 실행하기 전에 맥북인지 NCP인지 확인한다.
- 기존 Supabase 데이터는 새 환경 검증이 끝날 때까지 삭제하지 않는다.
- HTTPS가 적용되기 전에는 실제 민감정보를 입력하지 않는다.

## 15. 스스로 확인하는 세 가지 질문

### 질문 1

사용자가 THE VAULT 화면을 보는 데 필요한 부분은 무엇인가?

**답:** 프론트엔드이며, 현재는 GitHub Pages가 전달하고 앞으로는 NCP가 전달할 수 있다.

### 질문 2

로그인, 조직 권한, TOTP 인증은 어디에서 처리되는가?

**답:** 현재는 Supabase에서 처리된다.

### 질문 3

GitHub는 사용자 비밀번호와 실제 서비스 데이터를 저장하는 서버인가?

**답:** 아니다. GitHub는 코드와 변경 이력을 저장한다. 사용자 데이터와 서버 비밀정보를 넣어서는 안 된다.

## 16. 지금 꼭 기억할 한 문장

> GitHub는 코드를 보관하고, NCP는 화면을 전달하며, Supabase는 로그인과 데이터를 처리한다.

## 관련 문서

- `작업순서 및 작업기록.MD`: 실제 작업의 날짜별 기록
- `TESSERA-DIGITAL-VAULT-MVP-실행계획.MD`: 제품과 MVP 구현 계획
- `랜딩페이지 실행 계획.MD`: 브랜드 랜딩페이지 설계 및 구현 계획
- `app/README.md`: Trust Lab의 기술 범위와 보안 경계
