# THE VAULT 브랜치 작업 매뉴얼

이 문서는 `groot5`가 현재 안정 버전과 새로운 모바일 친화 버전을 혼동하지 않고 작업하기 위한 초보자용 안내서다.

## 1. 브랜치는 무엇인가

브랜치는 같은 프로젝트에서 갈라진 별도의 작업 흐름이다. 책으로 비유하면 원본 원고를 보존한 채 복사본에서 새로운 편집을 시험하는 것과 같다.

- `main`: 지금까지 검증하고 공개한 기준 버전
- `codex/mobile-friendly-v2`: 친근하고 상용 서비스에 가까운 모바일 UX 실험 버전

V2 브랜치에서 화면을 크게 바꾸더라도 `main`의 코드는 바뀌지 않는다. V2가 더 좋다고 판단됐을 때만 검토 후 `main`에 합친다.

## 2. 지금 어느 브랜치인지 확인하기

터미널에서 프로젝트 폴더로 이동한다.

```bash
cd /Users/groot5/Documents/ChatGPT/vault
git status -sb
```

첫 줄이 다음처럼 표시된다.

```text
## codex/mobile-friendly-v2
```

이 문장이 현재 작업 중인 브랜치다. 파일을 수정하기 전에는 항상 이 한 줄을 확인한다.

## 3. 모바일 친화 V2에서 계속 작업하기

먼저 저장하지 않은 변경이 없는지 확인한다.

```bash
git status --short
```

아무것도 나오지 않으면 안전하게 브랜치를 전환할 수 있다.

```bash
git switch codex/mobile-friendly-v2
git pull --ff-only origin codex/mobile-friendly-v2
```

그 다음 Codex에게 `현재 mobile-friendly-v2 브랜치인지 확인하고 계속해줘`라고 요청하면 된다.

## 4. 기존 안정 버전으로 돌아가기

V2 작업이 모두 커밋된 상태에서 다음 명령을 사용한다.

```bash
git switch main
git pull --ff-only origin main
```

공개 중인 기준 버전을 수정하려는 것이 아니라면 `main`에서는 실험적인 화면 변경을 하지 않는다.

## 5. 맥북과 집 PC에서 이어서 작업하기

다른 컴퓨터에서는 먼저 GitHub의 최신 브랜치 정보를 받는다.

Windows에서는 PowerShell이나 Git Bash를 열고 먼저 저장소가 있는 폴더로 이동한다. 경로는 설치 위치에 따라 다르므로 자신의 실제 폴더를 사용한다.

```powershell
cd C:\Users\사용자이름\Documents\vault
```

그 다음 명령은 Mac과 동일하다.

```bash
git fetch origin
git switch codex/mobile-friendly-v2
git pull --ff-only origin codex/mobile-friendly-v2
```

해당 브랜치가 그 컴퓨터에 아직 없다면 다음 명령을 한 번만 사용한다.

```bash
git switch --track origin/codex/mobile-friendly-v2
```

작업을 마치면 반드시 커밋하고 GitHub에 올린 뒤 다른 컴퓨터로 이동한다.

```bash
git status --short
git add 변경한파일
git commit -m "변경 목적"
git push
```

처음 V2 브랜치를 GitHub에 올리는 경우에만 `git push -u origin codex/mobile-friendly-v2`를 사용한다. 이후부터는 `git push`만 입력하면 된다.

## 6. 두 버전을 동시에 보고 싶을 때

Git worktree를 사용하면 안정 버전과 V2를 서로 다른 폴더에서 동시에 열 수 있다. 브랜치 개념이 익숙해지기 전에는 Codex에게 다음과 같이 요청하는 방법을 권장한다.

```text
main과 mobile-friendly-v2를 동시에 비교할 수 있도록 Git worktree를 안전하게 만들어줘.
```

직접 만들 때는 한 브랜치를 현재 폴더에서 사용 중이지 않아야 하므로, 상태 확인과 전환 순서가 중요하다.

## 7. 공개 배포와 브랜치는 다르다

브랜치를 GitHub에 올리는 것만으로 V2가 기존 공개 주소에 나타나지는 않는다.

- 현재 공개 페이지: `main`을 기준으로 배포
- V2 브랜치: GitHub에 백업되지만 별도 미리보기 배포 전에는 공개 페이지를 바꾸지 않음

V2를 사람들에게 보여주려면 별도 미리보기 주소를 만들거나, 검토 후 `main`에 합쳐야 한다.

현재 V2 비교용 주소는 다음과 같다.

```text
http://110.165.18.205/preview/mobile-v2/app/
```

이 주소는 `main`과 분리된 NCP 웹 루트에서 제공된다. 현재는 HTTP IP 주소이므로 화면과 기본 서버 응답을 확인하는 용도다. PWA 설치, 패스키, 실사용 Web Crypto 검증은 공식 도메인과 HTTPS 적용 후 진행한다.

## 8. 가장 중요한 안전 규칙

1. 작업 전 `git status -sb`로 현재 브랜치를 확인한다.
2. 브랜치 전환 전 `git status --short`가 비어 있는지 확인한다.
3. 한 컴퓨터에서 작업을 마치면 커밋과 push를 끝낸다.
4. 다른 컴퓨터에서는 작업 전 pull을 먼저 한다.
5. 충돌 메시지가 나오면 임의로 덮어쓰지 말고 Codex에게 화면과 오류를 보여준다.
6. V2를 `main`에 합치는 작업은 두 버전의 기능 검증이 끝난 뒤 진행한다.

## 9. 이번 작업에서 기억할 개념

- 브랜치: 서로 영향을 주지 않는 작업 흐름
- 커밋: 현재 상태의 저장 지점
- push: 로컬 커밋을 GitHub에 백업
- pull: GitHub의 최신 커밋을 현재 컴퓨터로 가져오기
- merge: 검증된 브랜치의 변경을 다른 브랜치에 합치기

명령어 자체보다 `현재 어느 브랜치에서 무엇을 바꾸는가`를 아는 것이 더 중요하다.
